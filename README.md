# Using dotfile in paying money...
# bashrc
A newer of archlinux to share the config...
=======
1.) function pAcup in bashrc
```terminal
>$pAcup -h
```

2.) function neofetch in bashrc

  -- install chafa (Image-to-text converter)

  ```terminal
  >$sudo pacman -S chafa
  ```
  -- backuping your ~/.config/neofetch

  -- downloading config fold and copying neofetch to ~/.config/
  ```terminal
  >$neofetch
  ```

3) foot terminal configure in config fold... 
